<% if resource.errors.any? %>
  <div class="alert alert-warning" role="alert">
    <h5>
      <span class="oi oi-warning" aria-hidden="true"></span> Problema(s) encontrado(s):
    </h5>
    <ul class="mb-0">
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
